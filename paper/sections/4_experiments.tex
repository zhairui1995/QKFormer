\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
\label{sec:setup}

We evaluate on CIFAR-10 using the QKFormer CIFAR architecture from the upstream
implementation~\cite{zhou2024qkformer}. Unless otherwise stated, diagnostics
use the trained checkpoint with $T=4$ that reaches $96.08\%$ best validation
top-1 accuracy, the first $T=1$ checkpoint that reaches $95.20\%$, and an
independently trained $T=1$ checkpoint with training seed 43 that reaches
$95.04\%$. All E3 adapter experiments freeze the trained QKFormer backbone and
train only the adapter table parameters.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
Checkpoint & Time steps & Best Acc@1 & Final Acc@1 \\
\midrule
QKFormer CIFAR-10 & 4 & 96.08 & 95.73 \\
QKFormer CIFAR-10 & 1 & 95.20 & 94.64 \\
QKFormer CIFAR-10 seed 43 & 1 & 95.04 & 94.76 \\
\bottomrule
\end{tabular}
}
\caption{QKFormer baselines used for QK-LUTFormer diagnostics. Accuracy is
reported in percent on CIFAR-10 validation.}
\label{tab:qkformer_baselines}
\end{table}

\subsection{Address Feasibility}
\label{sec:address_feasibility}

Table~\ref{tab:e0_feasibility} reports Q/K address occupancy diagnostics. Both
$T=1$ checkpoints keep similar address coverage to $T=4$ while lowering
conditional response variance, making $T=1$ a useful stress setting for testing
whether the address boundary itself carries structure.

\begin{table}[t]
\centering
\footnotesize
\begin{tabular}{lccc}
\toprule
Setting & Coverage & Singleton frac. & Cond. var. \\
\midrule
$T=4$ & 0.5993 & 0.0865 & 0.0713 \\
$T=1$ & 0.5805 & 0.0794 & 0.0549 \\
$T=1$ seed 43 & 0.5789 & 0.0822 & 0.0498 \\
\bottomrule
\end{tabular}
\caption{E0 address feasibility diagnostics. Lower conditional variance
indicates that responses are more predictable within Q/K-derived buckets.}
\label{tab:e0_feasibility}
\end{table}

\subsection{Split-Aware Reconstruction}
\label{sec:e1_results}

Table~\ref{tab:e1_recon} shows held-out response reconstruction. Address LUT
prototypes reduce MSE by $3.58\%$ relative to the global mean baseline, while
candidate/background means are nearly identical to the global mean.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcc}
\toprule
Predictor & MSE & Rel. MSE reduction \\
\midrule
Global mean & 0.07146 & 0.00 \\
Candidate/background mean & 0.07144 & 0.03 \\
Address LUT & \textbf{0.06887} & \textbf{3.58} \\
\bottomrule
\end{tabular}
}
\caption{E1 split-aware reconstruction on held-out validation batches. Relative
reduction is reported in percent against the global mean baseline.}
\label{tab:e1_recon}
\end{table}

\subsection{Forward-Only Replacement Diagnostics}
\label{sec:e2_results}

Forward-only replacement is useful diagnostically but not sufficient as a final
method. Stage1-only replacement is slightly positive on full validation, while
stage2 and combined stage1+stage2 replacement are negative. Further control
sweeps show that global smoothing nearly matches address LUT in T=4 settings,
so the forward-only path alone does not establish an address-specific claim.

\begin{table}[t]
\centering
\footnotesize
\begin{tabular}{lcc}
\toprule
Targets & Baseline Acc@1 & Replacement Acc@1 \\
\midrule
Stage1 only & 95.73 & \textbf{95.88} \\
Stage2 only & 95.73 & 95.59 \\
Stage1+Stage2 & 95.73 & 95.54 \\
\bottomrule
\end{tabular}
\caption{E2 full-validation forward-only replacement. Accuracy is in percent.
The result motivates using trainable residual adapters rather than pure
prototype replacement.}
\label{tab:e2_stage}
\end{table}

\subsection{Trainable Residual LUT Adapter}
\label{sec:e3_results}

The conservative E3 adapter uses fixed $\alpha$, two training epochs, 128
calibration batches, 128 training batches, full validation, and target module
\texttt{stage1.0.tssa}. The first $T=1$ checkpoint made address LUT positive
on average, but a repeat on the independent seed-43 checkpoint showed that
$\alpha=0.1$ is too strong and seed-dependent. We therefore run an alpha
sensitivity sweep on the independent checkpoint.

\begin{table*}[t]
\centering
\footnotesize
\begin{tabular}{lccccc}
\toprule
Mode & $\alpha$ & Seed 42 & Seed 43 & Seed 44 & Mean Acc@1 delta \\
\midrule
Address LUT & 0.025 & +0.14 & +0.16 & +0.12 & \textbf{+0.1400} \\
Global mean & 0.025 & -0.12 & +0.03 & +0.21 & +0.0400 \\
Token-channel LUT & 0.025 & +0.13 & -0.12 & -0.01 & +0.0000 \\
\midrule
Address LUT & 0.050 & -0.02 & -0.16 & -0.07 & -0.0833 \\
Global mean & 0.050 & +0.02 & -0.24 & +0.03 & -0.0633 \\
Token-channel LUT & 0.050 & +0.04 & -0.05 & -0.11 & -0.0400 \\
\midrule
Address LUT & 0.100 & +0.15 & -0.43 & -0.04 & -0.1067 \\
Global mean & 0.100 & -0.05 & -0.17 & +0.01 & -0.0700 \\
Token-channel LUT & 0.100 & -0.17 & -0.17 & +0.08 & -0.0867 \\
\bottomrule
\end{tabular}
\caption{T=1 E3 alpha sensitivity on the independent seed-43 checkpoint. Values
are replacement minus baseline top-1 accuracy deltas in percent. The
Q/K-address LUT has a stable low-disturbance window at $\alpha=0.025$, while
larger residual weights become negative and seed-dependent.}
\label{tab:e3_t1_multiseed}
\end{table*}

\subsection{Current Claim Boundary}
\label{sec:claim_boundary}

These experiments support the claim that Q/K binary addresses contain useful
structure for low-disturbance residual LUT adapters in a $T=1$ boundary
setting. They do not yet support claims about hardware speed, energy reduction,
ImageNet accuracy, or a production full-LUT QKFormer wrapper. The current
accuracy evidence is strongest at $\alpha=0.025$ and should be paired with
additional shuffled-address controls before making a broader address-specific
claim.
