\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
\label{sec:setup}

We evaluate the QKFormer CIFAR architecture~\cite{zhou2024qkformer} on CIFAR-10
and CIFAR-100. CIFAR-10 includes one $T=4$ checkpoint and three independently
trained $T=1$ checkpoints. CIFAR-100 includes independently trained $T=1$ and
$T=4$ checkpoints. Adapter-gate experiments freeze the backbone and train only
the small lookup probe.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
Checkpoint & Time steps & Best Acc@1 & Final Acc@1 \\
\midrule
QKFormer CIFAR-10 & 4 & 96.08 & 95.73 \\
QKFormer CIFAR-10 & 1 & 95.20 & 94.64 \\
QKFormer CIFAR-10 seed 43 & 1 & 95.04 & 94.76 \\
QKFormer CIFAR-10 seed 44 & 1 & 95.08 & 94.54 \\
QKFormer CIFAR-100 seed 42 & 1 & 77.76 & 77.40 \\
QKFormer CIFAR-100 seed 42 & 4 & 81.23 & 81.16 \\
\bottomrule
\end{tabular}
}
\caption{QKFormer baselines used for QK-LUTFormer diagnostics. Accuracy is
reported in percent on the corresponding validation split.}
\label{tab:qkformer_baselines}
\end{table}

\subsection{Address Feasibility}
\label{sec:address_feasibility}

Table~\ref{tab:e0_feasibility} reports Q/K address occupancy diagnostics. The
$T=1$ checkpoints keep similar address coverage to $T=4$ while generally lowering
conditional response variance, making $T=1$ a useful stress setting for testing
whether the address boundary itself carries structure.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
Setting & Coverage & Singleton frac. & Cond. var. \\
\midrule
$T=4$ & 0.5993 & 0.0865 & 0.0713 \\
$T=1$ & 0.5805 & 0.0794 & 0.0549 \\
$T=1$ seed 43 & 0.5789 & 0.0822 & 0.0498 \\
$T=1$ seed 44 & 0.5840 & 0.0805 & 0.0529 \\
CIFAR-100 $T=1$ & 0.5900 & 0.0768 & 0.0747 \\
CIFAR-100 $T=4$ & 0.6128 & 0.0790 & 0.0894 \\
\bottomrule
\end{tabular}
}
\caption{E0 address feasibility diagnostics. Lower conditional variance
indicates that responses are more predictable within Q/K-derived buckets.}
\label{tab:e0_feasibility}
\end{table}

\subsection{Split-Aware Reconstruction}
\label{sec:e1_results}

Table~\ref{tab:e1_recon} shows held-out response reconstruction. Address LUT
prototypes reduce MSE relative to the global mean on both datasets, by
$3.71\%$ on CIFAR-10 and $4.42\%$ on CIFAR-100. Each value is the mean over
three randomized 128-batch calibration subsets evaluated on the complete
validation split; the standard deviations are only $0.0038$ and $0.0042$
percentage points, respectively.
The mean reduction is largest in stage1 ($10.04\%$), remains positive in
stage2 ($3.96\%$), and is $1.85\%$ in stage3. The address LUT also consistently
beats the coarse token/channel LUT ($3.42\%$ overall), while shuffled address
alignment worsens MSE by $15.82\%$. Candidate/background means remain nearly
identical to the global mean.
The independently trained CIFAR-100 $T=4$ checkpoint preserves the same
ordering: address lookup reduces MSE by $2.84\%$, compared with $2.24\%$ for
token/channel lookup, while shuffled alignment degrades it by $13.74\%$.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Dataset & Global & Cand./back. & Token/channel & Address LUT & Rel. reduction \\
\midrule
CIFAR-10 & 0.07129 & 0.07127 & 0.06926 & \textbf{0.06861} & \textbf{3.71} \\
CIFAR-100 & 0.07853 & 0.07850 & 0.07613 & \textbf{0.07547} & \textbf{4.42} \\
CIFAR-100 $T=4$ & 0.09314 & 0.09312 & 0.09120 & \textbf{0.09072} & \textbf{2.84} \\
\bottomrule
\end{tabular}
}
\caption{E1 split-aware reconstruction on held-out validation batches. MSE is
aggregated over the instrumented modules; relative reduction is reported in
percent against the corresponding global mean baseline. The first two rows
report means over three randomized calibration subsets and full validation;
the CIFAR-100 $T=4$ row is the registered 128-batch calibration run.}
\label{tab:e1_recon}
\end{table}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig3_e1_reconstruction.pdf}
  \caption{E1 held-out response reconstruction under address controls. (a)
  Correct Q/K addresses consistently reduce MSE more than coarse token/channel
  lookup across three CIFAR-10 checkpoints and three CIFAR-100 calibration
  seeds. (b) Shuffling the address--prototype alignment makes reconstruction
  worse than the global mean baseline, showing that the result is not explained
  by table capacity alone. Values are relative MSE reductions against the
  global mean and are read directly from result JSON files.}
  \label{fig:e1-reconstruction}
\end{figure*}

For CIFAR-10, the address, token/channel, candidate/background, and shuffled
relative reductions are $3.7080\%$, $2.7941\%$, $0.0329\%$, and $-17.8260\%$.
The corresponding CIFAR-100 values are $4.4247\%$, $3.4246\%$, $0.0356\%$,
and $-15.8216\%$. The ordering holds for every calibration seed and in all
three stages on both datasets, supporting an address-specific reconstruction
claim rather than a generic table-capacity effect.

We additionally repeat the same full-validation reconstruction control on
three independently trained CIFAR-10 $T=1$ checkpoints
(Table~\ref{tab:e1_checkpoints}). Address LUT reduction varies with the
backbone, but it exceeds the token/channel control for every checkpoint. The
mean address advantage over token/channel is $1.1318$ percentage points.

\begin{table}[t]
\centering
\footnotesize
\begin{tabular}{lccc}
\toprule
Training seed & Address & Token/channel & Shuffled \\
\midrule
42 & 3.7597 & 2.8026 & -18.8859 \\
43 & 5.9361 & 4.4966 & -17.5642 \\
44 & 4.6503 & 3.6514 & -17.0855 \\
\bottomrule
\end{tabular}
\caption{Relative E1 MSE reduction (percent) on independently trained CIFAR-10
$T=1$ checkpoints. Each row uses the same randomized 128-batch calibration
protocol and full validation.}
\label{tab:e1_checkpoints}
\end{table}

Table~\ref{tab:e1_calibration} measures calibration data efficiency on
CIFAR-100 over calibration seeds 42--44. Eight batches already recover
$3.78\%$ relative MSE reduction, approximately $85\%$ of the $4.48\%$ obtained
with 512 batches. Address LUT remains above token/channel for every size and
seed, and mean validation address hit rate exceeds $99.79\%$ even at the
smallest setting.

\begin{table}[t]
\centering
\footnotesize
\begin{tabular}{rccc}
\toprule
Calib. batches & Address & Token/channel & Hit rate \\
\midrule
8 & 3.7840 & 3.3258 & 99.7982 \\
32 & 4.2671 & 3.4027 & 99.9558 \\
128 & 4.4247 & 3.4246 & 99.9907 \\
512 & 4.4752 & 3.4386 & 99.9981 \\
\bottomrule
\end{tabular}
\caption{CIFAR-100 E1 calibration-size sweep, averaged over calibration seeds
42--44. Address and token/channel values are relative MSE reductions in
percent; hit rate is the percentage of validation samples whose address has
sufficient calibration support.}
\label{tab:e1_calibration}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/fig4_calibration_sweep.pdf}
  \caption{CIFAR-100 calibration-size sweep for E1 reconstruction. (a)
  Correct Q/K addresses remain above token/channel lookup from 8 to 512
  calibration batches. (b) The validation address hit rate is high even at 8
  batches and saturates with more calibration data. Curves show means over
  calibration seeds 42--44; error bars show population standard deviations.}
  \label{fig:calibration-sweep}
\end{figure}

\subsection{Hierarchical Backoff Under Calibration Scarcity}
\label{sec:e5_results}

Table~\ref{tab:e5_backoff} evaluates the support-aware hierarchical backoff
LUT on CIFAR-100 $T=1$ with calibration sizes from 1 to 512 batches and
calibration seeds 42--44. The hierarchy is explicit about fallback behavior:
the fallback fractions sum to one for every run, and no validation sample is
silently assigned to an unsupported missing address. From 8 batches onward,
hierarchical backoff beats token/channel lookup for every seed and retains at
least $80\%$ of the full-address reconstruction gain. Shuffled full-address
lookup remains strongly worse than the correct hierarchy at every size,
confirming that address--prototype alignment matters.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{rccccc}
\toprule
Calib. & Hier. & Full addr. & Token/ch. & Shuffled & Eff. entries \\
\midrule
1 & 0.3178 & 0.4179 & 2.5493 & -32.2973 & 0.3305 \\
2 & 2.1445 & 2.1789 & 3.0002 & -26.0841 & 0.3566 \\
4 & 3.1962 & 3.2150 & 3.2261 & -24.9300 & 0.3808 \\
8 & 3.7742 & 3.7840 & 3.3258 & -20.7501 & 0.4040 \\
32 & 4.2667 & 4.2671 & 3.4027 & -18.0293 & 0.4477 \\
128 & 4.4248 & 4.4247 & 3.4246 & -15.8216 & 0.4822 \\
512 & 4.4752 & 4.4752 & 3.4386 & -13.5117 & 0.5107 \\
\bottomrule
\end{tabular}
}
\caption{E5 hierarchical backoff reconstruction on CIFAR-100 $T=1$. Values are
mean relative MSE reductions in percent over calibration seeds 42--44. Effective
entries are supported hierarchy entries divided by the compact full-address
space; lower is better.}
\label{tab:e5_backoff}
\end{table}

The hierarchical gate outcome is therefore \emph{partial}. The method passes
the reconstruction and interpretability checks: 21/21 runs completed, the
hierarchy beats token/channel for all calibration sizes $\geq 8$, shuffled
lookup is always worse, fallback distributions are valid, and the hierarchy
retains the full-address gain at practical calibration sizes. It fails the
compact-table gate because effective supported entries are $40.4\%$--$51.1\%$
of the compact full-address space for sizes $\geq 8$.

Table~\ref{tab:e7_subspace} then evaluates the subspace-decoupled residual LUT.
The compact token/channel+Q/gate estimator beats token/channel lookup for every
calibration size and seed while using only $8.83\%$ supported entries. Adding
the K subtable remains below the $25\%$ entry budget and retains most of the
full-address gain. Both subspace variants beat their shuffled-subspace controls
in every run, showing that the gain depends on aligned Q/K sub-address
semantics rather than merely on residual table values.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{rccccc}
\toprule
Calib. & Token/ch. & TC+Q & TC+QK & TC+Q entries & TC+QK entries \\
\midrule
8 & 3.3258 & 3.7502 & 4.0088 & 0.0883 & 0.2020 \\
32 & 3.4027 & 3.8463 & 4.1357 & 0.0883 & 0.2047 \\
128 & 3.4246 & 3.8744 & 4.1712 & 0.0883 & 0.2054 \\
512 & 3.4386 & 3.8884 & 4.1866 & 0.0883 & 0.2057 \\
\bottomrule
\end{tabular}
}
\caption{E7 subspace-decoupled reconstruction on CIFAR-100 $T=1$. Values are
mean relative MSE reductions in percent over calibration seeds 42--44. Entry
fractions are supported scalar subtable entries divided by supported compact
full-address entries; they are entry-count proxies, not measured hardware
storage. Both TC+Q and TC+QK pass all shuffled-subspace controls.}
\label{tab:e7_subspace}
\end{table}

\subsection{Downstream Accuracy Gate}
\label{sec:e3_results}

Reconstruction alone does not establish a useful classifier. We therefore
evaluate a matched CIFAR-100 $T=4$ gate on the best 81.23\% QKFormer
checkpoint. The comparison fixes stage1, $\alpha=0.025$, one adapter epoch, 128
prototype-calibration and training batches, three adapter seeds, and the
upstream AMP full-validation loader. Prototype, adapter, and gate calibration
use three disjoint train-index partitions. The frozen backbone is kept in
evaluation mode, and an $\alpha=0$ identity preflight must reproduce the
checkpoint before any adapter comparison is admitted. We compare aligned,
global, and same-capacity shuffled-address probes under exactly the same
optimization and gate-selection budget.

Table~\ref{tab:t4_accuracy_gate} reports both unconditional replacement and the
calibration-only deterministic gate. The paired backbone reevaluation is
$81.22\%$ Acc@1 (the training log reports a best checkpoint value of
$81.23\%$). Unconditional replacement remains unstable. In contrast, the
aligned selective gate reaches $81.69\%$, $81.93\%$, and $81.56\%$, improving
the paired baseline for every adapter seed. Its $81.73\%$ mean exceeds global
gating by $0.26$ points and shuffled-address gating by $0.11$ points.

\begin{table}[t]
\centering
\footnotesize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
Mode & Raw mean & Gate 42 & Gate 43 & Gate 44 & Gate mean & Min. $\Delta$ \\
\midrule
Paired QKFormer & 81.22 & 81.22 & 81.22 & 81.22 & 81.22 & -- \\
Global mean & 80.91 & 81.61 & 81.27 & 81.51 & 81.46 & +0.05 \\
Global + centered address & 81.10 & 81.69 & \textbf{81.93} & 81.56 & \textbf{81.73} & +0.34 \\
Global + centered shuffled & 81.19 & \textbf{81.88} & 81.43 & 81.54 & 81.62 & +0.21 \\
\bottomrule
\end{tabular}
}
\caption{Matched one-epoch CIFAR-100 $T=4$ downstream gate. Entries are Acc@1
in percent. ``Raw mean'' applies the residual to every validation sample. Gate
thresholds are fitted independently for each adapter seed using a disjoint
train partition; validation labels are never used. Min. $\Delta$ is the minimum
gated improvement over the paired backbone across seeds.}
\label{tab:t4_accuracy_gate}
\end{table}

The one-epoch gate passes the predeclared checkpoint-level criterion: aligned
gating exceeds the saved QKFormer best, improves all three adapter seeds, and
separates from both matched controls in the mean. The aligned--shuffled gap is
not positive for every seed, however, so address-specific classification
superiority is not yet backbone-stable. A two-epoch follow-up reaches a best
gated Acc@1 of $82.02\%$ and an aligned mean of $81.75\%$, but separates from
the stronger global gate by only $0.04$ points. We therefore claim calibrated
selective utility on this checkpoint and reserve checkpoint-independent
accuracy claims for the ongoing independent-backbone replication.

\subsection{Current Claim Boundary}
\label{sec:claim_boundary}

These experiments support the claim that Q/K binary addresses contain
measurable response structure and that semantic subspace tables can retain most
of this structure under a fixed entry-count budget. A disjoint-calibration
margin gate further supports selective classification utility on the evaluated
checkpoint. Only protocol-v5 runs that preserve the frozen backbone state,
separate all calibration partitions, and reproduce the upstream evaluation
protocol are admitted to this result. Independent-backbone replication remains
pending, and the evidence does not support hardware speed, energy reduction,
ImageNet accuracy, or a production full-LUT QKFormer wrapper.
